[package]
name = "boss-gateway"
description = "Boss gateway — reverse proxy, session auth, and file-backed credentials"
version.workspace = true
edition.workspace = true

[[bin]]
name = "boss-gateway"
path = "src/main.rs"

[dependencies]
boss-core = { path = "../boss-core" }
# The auth-event staging path (docs/architecture-decisions.md
# §Policy & auth): the
# gateway's one database dependency, scoped to PgOutboxRecorder +
# a 2-connection lazy pool. Pool only — no macros, no migrations.
boss-clock-client = { path = "../boss-clock-client" }
boss-events = { path = "../boss-events", features = ["postgres"] }
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio", "postgres"] }
async-trait = { workspace = true }
boss-classes-client = { path = "../boss-classes-client" }
boss-ports = { path = "../boss-ports" }
anyhow = { workspace = true }
axum = "0.8"
rand = "0.10"
chrono = { workspace = true }
reqwest = { version = "0.13", default-features = false, features = ["json", "rustls", "stream"] }
base64 = "0.22"
webauthn-rs = { version = "0.5", features = ["danger-allow-state-serialisation", "danger-user-presence-only-security-keys"] }
webauthn-rs-core = "0.5"
hmac = "0.13"
sha2 = "0.11"
subtle = "2"
futures = { workspace = true }
bytes = "1"
async-stream = "0.3"
http-body-util = "0.1"
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
hdrhistogram = "7"
jsonwebtoken = "10"
# Argon2id password hashing for the file-backed local-auth path.
# PHC string format so the on-disk credentials file stays
# portable across versions.
argon2 = "0.5"
# TOML for the credentials file format. workspace = true picks up
# the same toml crate boss-jobs / boss-sim already use.
toml = { workspace = true }

[[bin]]
# CLI for managing the local-auth credentials file. Used by the
# OSS quickstart's bootstrap path + by operators onboarding new
# users until the SPA admin UX lands.
name = "boss-auth"
path = "src/bin/boss_auth.rs"

[dev-dependencies]
tempfile = "3"
# oneshot() for the role-header middleware probe tests.
tower = { version = "0.5", features = ["util"] }

[lints]
workspace = true
