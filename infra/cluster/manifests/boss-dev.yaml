# boss-dev — a development pod that runs on cluster hardware.
#
# WHY THIS EXISTS. Development was happening on a laptop, and on
# 2026-08-15 that laptop filled its disk: 4,603 scratch test databases,
# 68G, root at 100% with 117Mi free, mid-session. The cluster has 28
# vCPU and ~80GB of RAM sitting next to the system under test. This
# moves builds and DB-backed tests onto it.
#
# THE SAFETY PROPERTY IS THE POINT, NOT A BONUS. `boss_testing::TestDb`
# connects to `postgres://boss:boss@127.0.0.1/postgres` and creates a
# scratch database per test. On a laptop that default is a loaded gun:
# open a `kubectl port-forward` to the cluster Postgres for an unrelated
# read, run the suite, and 127.0.0.1 IS production — which is exactly
# what happened on 2026-08-14, when a `cargo test --workspace` created
# 582 databases on the production volume and crashed Postgres into
# recovery. Inside this pod, 127.0.0.1:5432 is the sidecar below and
# cannot be anything else. The failure mode is removed by construction
# rather than by remembering.
#
# It also closes a version skew nobody had noticed: the laptop ran
# Postgres 14 (homebrew) while production runs 16, so every local DB
# test was run against a different major than the one it ships to.
#
# ACCESS
#   kubectl exec -it -n boss-dev deploy/boss-dev -- bash
#
# ONE-TIME BOOTSTRAP (secrets never live in this directory — see
# README.md, and the registry secret is namespaced so `boss`'s copy
# does not serve here):
#   kubectl get secret forgejo-registry -n boss -o yaml \
#     | sed 's/namespace: boss/namespace: boss-dev/' \
#     | kubectl apply -n boss-dev -f -
#
# NOT IN SCOPE FOR v1, deliberately:
#   - A second CI runner. David, 2026-08-15: get the current system
#     running consistently first. This shares CI's IMAGE, which is a
#     different thing from sharing its job — see the image note below.
#   - Running the agent itself in-cluster. That needs credentials in the
#     cluster, which is David's call, not a manifest change.
#   - A NetworkPolicy fencing this namespace off from `boss`. It is the
#     right next step and it is omitted on purpose: an allow-list egress
#     policy that gets the pod/service CIDRs wrong silently breaks
#     `cargo fetch` and reads as "the dev box is broken", and an
#     untested policy is worse than a documented gap. The sidecar above
#     is what actually removes the incident class; the policy is defence
#     in depth and deserves its own car with a reachability test.
---
apiVersion: v1
kind: Namespace
metadata:
  name: boss-dev
  labels: {app.kubernetes.io/part-of: boss, boss.dev/purpose: development}
---
# A single-replica storage class for disposable volumes.
#
# The default `longhorn` class replicates three times, which is correct
# for the system of record and wrong for a workspace: this volume holds
# a git clone and a package cache, both reproducible from their source
# in minutes. Three replicas would cost 3x the space and, worse, put
# every build's write amplification on the replication path.
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: longhorn-dev-disposable
provisioner: driver.longhorn.io
allowVolumeExpansion: true
reclaimPolicy: Delete
volumeBindingMode: Immediate
parameters:
  numberOfReplicas: "1"
  # Keep the single replica on the node running the pod: a dev volume
  # has no availability requirement, and local reads are the point.
  dataLocality: "best-effort"
  staleReplicaTimeout: "30"
  fsType: "ext4"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: boss-dev-work
  namespace: boss-dev
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn-dev-disposable
  resources:
    requests:
      # The clone plus CARGO_HOME's registry cache. The build target dir
      # is NOT here — see the emptyDir below.
      storage: 40Gi
---
# The gate credential (0b1b32f9, greenlit 2026-08-31). The dev session
# earns exactly the verbs `boss gate` uses and nothing else: gate
# runner Jobs live in THIS namespace (infra/gate-runner), so the Role
# is namespace-scoped and the blast radius is "can run and watch gate
# Jobs here" — not the cluster. Reads of other namespaces, secrets,
# or RBAC objects are deliberately absent; widening this Role is a
# David-reviewed change like any credential (token admin is his).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dev-session
  namespace: boss-dev
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: dev-session-gates
  namespace: boss-dev
rules:
  # The runner Job: created per gate, previous one deleted first
  # (gate.rs), watched to a verdict.
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["create", "get", "list", "watch", "delete"]
  # The runner's pod, for status + logs while the gate runs.
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
  # gate-runner.yaml's PVC document rides `kubectl apply`, which
  # needs read-modify-write on the one object it manages.
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs: ["get", "create", "patch", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: dev-session-gates
  namespace: boss-dev
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: dev-session-gates
subjects:
  - kind: ServiceAccount
    name: dev-session
    namespace: boss-dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: boss-dev
  namespace: boss-dev
spec:
  replicas: 1
  selector:
    matchLabels: {app: boss-dev}
  # Recreate, for the same reason boss.yaml uses it: the workspace is a
  # ReadWriteOnce volume, so a surge pod would block on multi-attach
  # while the old one holds it. RollingUpdate here deadlocks.
  strategy: {type: Recreate}
  template:
    metadata:
      labels: {app: boss-dev}
    spec:
      # Q1, measured 2026-08-16 rather than assumed. A cold
      # `cargo test --all-features` needs ~74 GB of target/ (train 49:
      # the forge host went 141 GB free to 67 GB), and target/ lives in
      # the `scratch` emptyDir below — node-local disk, not the 40 Gi
      # workspace PVC. The three nodes allocate 212 / 212 / 417 GiB of
      # ephemeral storage, so the headroom is real; cp-2 showed 188 GB
      # free with the pod already on it.
      #
      # Pinned anyway, because "there is room on all three" and "this
      # build has room" are different claims once the system of record's
      # Longhorn replicas share the disk. The build node is the
      # choice, selected by role label rather than hostname: 12 cores
      # against 8, and the workspace PVC's single replica already has
      # dataLocality best-effort to wherever the pod runs — so pinning
      # to where it already is costs no rebuild, and moving would.
      # PREFER the build node, do not require it. A hard nodeSelector
      # here means the build role is a single point of failure: when
      # w-1 was cordoned for an NVMe thermal fault, a hard pin would
      # have left this with no schedulable node and stopped gating
      # entirely. Preferred affinity degrades to a slower build on a
      # control plane instead of no build at all.
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - {key: boss.dev/purpose, operator: In, values: ["build"]}
      imagePullSecrets:
        - {name: forgejo-registry}
      # The dev session holds ONE cluster credential: the dev-session
      # ServiceAccount, bound to the gates Role above — create/watch
      # gate Jobs in this namespace, nothing else. The blast-radius
      # argument that kept this false still governs the ROLE's size;
      # the greenlit trade (0b1b32f9) is that pod-launched gates beat
      # a hand-carried gate verb on another machine.
      serviceAccountName: dev-session
      automountServiceAccountToken: true
      # PID 1 must reap. The dev container's entrypoint is `sleep`,
      # which never wait()s, so daemonized children became unkillable
      # zombies during the ssh door's first bring-up (2026-08-30).
      # Sharing the process namespace makes pause PID 1, and pause
      # reaps. Costs process visibility between dev and postgres,
      # which nothing here minds.
      shareProcessNamespace: true
      securityContext:
        # The workspace volume is group-owned so a non-root build can
        # write to it, matching boss.yaml's posture for boss-auth.
        fsGroup: 1500
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: dev
          # THE SAME IMAGE CI RUNS. Not a copy of its tool list — the
          # image itself, so "works on the dev box" and "passes the
          # gate" cannot drift apart. required-tools.txt is the manifest
          # the locomotive check reads, and it is satisfied here by
          # construction. Sharing the image is not the same as running a
          # second runner; only the toolchain definition is shared.
          image: 10.20.0.15:3000/david/boss-ci:rust1.96
          # ALWAYS, because rust1.96 is a MUTABLE tag. build-image
          # rebuilds and re-pushes it whenever infra/forge/boss-ci/
          # changes, so `IfNotPresent` pins this pod to whatever the
          # node happened to cache first — which it did: the image
          # gained Node 20 and tmux, and the pod went on running Node
          # 18 with no tmux because cp-2 had the old layer.
          #
          # The CI runner hit the identical defect the same evening
          # (locomotive caught it: stamp d1ccc81a against a tree
          # wanting 3104b286). Two consumers, one wrong assumption —
          # that a tag is immutable. Pinning by digest would be the
          # stronger fix and needs the digest threaded from the build;
          # this is the one-line version that makes the pod track the
          # tag it names.
          imagePullPolicy: Always
          command: ["sleep", "infinity"]
          securityContext:
            allowPrivilegeEscalation: false
            # SETGID/SETUID and nothing else: the ssh door (02f4a2ee,
            # David 2026-08-30: "I just want to do the ssh link") needs
            # to spawn a session as the connecting user. Dropbear over
            # OpenSSH precisely because sshd's mandatory preauth chroot
            # would ALSO need SYS_CHROOT — measured on the live pod:
            # sshd failed chroot(), dropbear failed only setgid, and
            # this pair is the whole difference.
            capabilities:
              drop: ["ALL"]
              add: ["SETGID", "SETUID"]
          ports:
            - {name: ssh, containerPort: 22}
          # The ssh door + session env, declared. Everything in here
          # was first done by hand on 2026-08-30 and then transcribed,
          # not invented: the systemd postinst neuter (its postinst
          # cannot run in a container and a dependency drags it in),
          # the apt cache redirect (the image's /var cache refuses
          # writes), and the .bashrc (the very first attach failed
          # because a plain exec shell had no PATH to the tools).
          # BEST-EFFORT by design: a bootstrap failure leaves the door
          # closed and the pod healthy — kubectl exec still works and
          # /work/ssh/postStart.log holds the evidence. Accepted debt:
          # apt runs on every pod start (network dependency); the
          # upgrade is baking dropbear into boss-ci, worth doing only
          # if boot flakiness is ever actually observed.
          lifecycle:
            postStart:
              exec:
                command:
                  - bash
                  - -c
                  - |
                    {
                    set -x
                    export DEBIAN_FRONTEND=noninteractive
                    printf '#!/bin/sh\nexit 0\n' > /var/lib/dpkg/info/systemd.postinst 2>/dev/null || true
                    printf 'APT::Sandbox::User "root";\n' > /etc/apt/apt.conf.d/99-pod-no-sandbox
                    mkdir -p /tmp/apt /work/ssh /root/.ssh /work/home
                    apt-get -o Dir::Cache::archives=/tmp/apt update -qq || true
                    apt-get -o Dir::Cache::archives=/tmp/apt install -y --no-install-recommends dropbear-bin openssh-sftp-server || true
                    [ -f /work/ssh/dropbear_ed25519_host_key ] || dropbearkey -t ed25519 -f /work/ssh/dropbear_ed25519_host_key
                    cp /etc/ssh-authorized/authorized_keys /root/.ssh/authorized_keys || true
                    chmod 700 /root/.ssh && chmod 600 /root/.ssh/authorized_keys || true
                    # BOTH homes: sessions arrive with HOME=/work/home
                    # (the durable home this design chose for claude
                    # state), so ~/.profile resolves on the PVC - the
                    # first door probe found login shells with the bare
                    # image PATH because these lived only in /root
                    # (3cca8c4d). Which home a session gets depends on
                    # its entry path, so write both.
                    for H in /root /work/home; do
                    cat > $H/.bashrc <<'RC'
                    export HOME=/work/home
                    export PATH=/work/tools/bin:/usr/local/cargo/bin:$PATH
                    export RUSTUP_HOME=/usr/local/rustup
                    export CARGO_TARGET_DIR=/scratch/target
                    cd /work/boss 2>/dev/null || true
                    RC
                    printf '[ -f ~/.bashrc ] && . ~/.bashrc\n' > $H/.profile
                    done
                    cat > /work/dev-session.sh <<'LS'
                    #!/usr/bin/env bash
                    export HOME=/work/home
                    export PATH=/work/tools/bin:/usr/local/cargo/bin:$PATH
                    export RUSTUP_HOME=/usr/local/rustup
                    export CARGO_TARGET_DIR=/scratch/target
                    cd /work/boss
                    exec tmux new-session -A -s dev
                    LS
                    chmod +x /work/dev-session.sh
                    # kubectl for `boss gate`, pinned by sha so CDN
                    # weather cannot ship a different binary (the
                    # a700d3a4 lesson). Best-effort: a failed download
                    # leaves gating impossible, never the pod broken.
                    # Lands on the PVC, so it downloads once, not per
                    # restart. Version matches the cluster line (1.33).
                    if [ ! -x /work/tools/bin/kubectl ]; then
                      curl -sfL -o /tmp/kubectl https://dl.k8s.io/release/v1.33.3/bin/linux/amd64/kubectl \
                        && echo "2fcf65c64f352742dc253a25a7c95617c2aba79843d1b74e585c69fe4884afb0  /tmp/kubectl" | sha256sum -c - \
                        && install -m 0755 /tmp/kubectl /work/tools/bin/kubectl || true
                    fi
                    dropbear -r /work/ssh/dropbear_ed25519_host_key -p 22 -s
                    } > /work/ssh/postStart.log 2>&1 || true
          workingDir: /work
          env:
            # The sidecar, by name rather than by luck. TestDb would
            # default to this anyway; setting it explicitly means a
            # future default change cannot silently redirect the suite.
            - {name: BOSS_TEST_POSTGRES_ADMIN_URL, value: "postgres://boss:boss@127.0.0.1:5432/postgres"}
            - {name: CARGO_HOME, value: /work/.cargo}
            # Build artefacts go to node-local disk, not to Longhorn.
            # A replicated network volume is the wrong medium for a
            # target dir that rewrites gigabytes on every build; losing
            # it on reschedule costs one cold build and nothing else.
            - {name: CARGO_TARGET_DIR, value: /scratch/target}
            - {name: GIT_CONFIG_GLOBAL, value: /work/.gitconfig}
            # GIT CREDENTIALS, DECLARED RATHER THAN SET UP.
            #
            # The pod could not clone: "could not read Username for
            # http://10.20.0.15:3000". It was bootstrapped once with a
            # git bundle over `kubectl cp`, which proved the
            # environment and left it unable to fetch.
            #
            # These three are git's own env-var config channel, so the
            # helper exists the moment the container starts. The
            # alternative — a `git config` line run by hand inside the
            # pod — writes to the PVC and therefore survives, which is
            # worse: it works until the volume is recreated and then
            # fails as a mystery. A manifest cannot be forgotten.
            - {name: GIT_CONFIG_COUNT, value: "1"}
            - {name: GIT_CONFIG_KEY_0, value: "credential.http://10.20.0.15:3000.helper"}
            - {name: GIT_CONFIG_VALUE_0, value: "!f() { echo username=david; echo password=$(cat /etc/forge/token); }; f"}
          # Headroom for a workspace build without being able to
          # starve the system of record sharing these nodes. Not
          # theoretical: on 2026-08-22 an unbounded six-gate cargo
          # chain starved etcd into failing readiness (18:10Z) and
          # then bounced cp-2's kubelet outright (19:17:49Z). A
          # bounded build dies alone.
          #
          # ONE resources block, deliberately: two cars racing through
          # one train each added their own on 2026-08-22, and the
          # duplicate YAML key meant the applied limits depended on
          # parser order. This is the collapse (CLAUDE.md 9a).
          resources:
            requests: {cpu: "2", memory: 4Gi}
            limits: {cpu: "8", memory: 16Gi}
          volumeMounts:
            - {name: work, mountPath: /work}
            - {name: scratch, mountPath: /scratch}
            - {name: forge-token, mountPath: /etc/forge, readOnly: true}
            - {name: ssh-authorized, mountPath: /etc/ssh-authorized, readOnly: true}
        - name: postgres
          # Matches production's major (the `boss` StatefulSet runs
          # postgres:16). A dev database on a different major is a test
          # of something other than what ships.
          image: postgres:16
          securityContext:
            allowPrivilegeEscalation: false
            # NOT `drop: ALL`, and the exception is deliberate: this
            # image's entrypoint starts as root, chowns PGDATA, and
            # execs gosu to become the postgres user. Dropping CHOWN
            # and SETUID/SETGID takes away the very capabilities it
            # uses to STOP being root, so the hardening would either
            # crash the container or keep it running as root. PGDATA is
            # a subdirectory of the mount (not the mount itself) so the
            # entrypoint can chmod it 0700 despite the pod's fsGroup
            # setgid bit, which Postgres refuses to start without.
            capabilities: {drop: ["NET_RAW"]}
          env:
            - {name: POSTGRES_USER, value: boss}
            - {name: POSTGRES_PASSWORD, value: boss}
            - {name: POSTGRES_DB, value: postgres}
            - {name: PGDATA, value: /pgdata/data}
          # emptyDir, emphatically: scratch databases accumulate — 4,603
          # of them in one day on the laptop — and the right lifetime for
          # them is "until the pod restarts". Persisting them would
          # rebuild the problem this pod exists to solve.
          volumeMounts:
            - {name: pgdata, mountPath: /pgdata}
            # Without this, /dev/shm is the container default 64Mi.
            # Parallel pg-backed gate suites blow through that:
            # "could not resize shared memory segment ... No space left
            # on device", the backend dies, and the whole database drops
            # into crash recovery MID-GATE — which read as three
            # different cars failing on 2026-08-22 before the common
            # cause surfaced. Memory-backed and capped at 1Gi, counted
            # against this container's 4Gi limit.
            - {name: pg-shm, mountPath: /dev/shm}
          resources:
            requests: {cpu: 250m, memory: 512Mi}
            limits: {cpu: "2", memory: 4Gi}
      volumes:
        # THE READ TOKEN, and read is the whole point.
        #
        # dev-node-checkout Q5, accepted: "Read-only, and never a push
        # credential. The conductor now publishes car branches itself
        # from its own clone, so a workspace has no reason to hold
        # write access to the forge — the thing it would have been for
        # is already solved."
        #
        # This mount was called `forge-push` when it was still meant to
        # let a session publish its own cars. That need disappeared the
        # moment `publish_car_branch` landed, and a secret whose name
        # claims more privilege than it holds is a trap for whoever
        # reads it next.
        #
        # `optional: true` stays: the Secret is created out-of-band
        # (README: "Secrets never live here"), and a required mount
        # would leave an unbootstrapped cluster with a pod stuck in
        # ContainerCreating and no explanation. Absent token = a pod
        # that cannot clone, which is a better failure than no pod.
        #
        #   kubectl create secret generic forge-read -n boss-dev \
        #     --from-literal=token=<a read:repository token>
        - name: forge-token
          secret: {secretName: forge-read, optional: true, defaultMode: 0400}
        - name: work
          persistentVolumeClaim: {claimName: boss-dev-work}
        - name: scratch
          emptyDir: {}
        - name: pgdata
          emptyDir: {}
        - name: pg-shm
          emptyDir: {medium: Memory, sizeLimit: 1Gi}
        # David's PUBLIC key, by name only (same posture as forge-read:
        # values never live in the tree). Public key material, so any
        # operator can load it:
        #   kubectl create secret generic boss-dev-ssh-authorized-keys \
        #     -n boss-dev --from-file=authorized_keys=<his id_ed25519.pub>
        # `optional: true` for the same reason as forge-read — an
        # absent Secret means a closed door, not a stuck pod.
        - name: ssh-authorized
          secret: {secretName: boss-dev-ssh-authorized-keys, optional: true, defaultMode: 0400}
---
# The ssh door's address: ssh://root@10.20.0.35 — macOS opens Terminal
# on that URL natively, which is the "launch my terminal from the
# website" answer (02f4a2ee) without any browser-terminal machinery.
# Reachable on the LAN directly; off-LAN via the boss-gcp WireGuard
# jump. Auth is David's ed25519 key only: no passwords, root key login.
apiVersion: v1
kind: Service
metadata:
  name: boss-dev-ssh
  namespace: boss-dev
  annotations:
    metallb.universe.tf/loadBalancerIPs: "10.20.0.35"
spec:
  type: LoadBalancer
  loadBalancerIP: 10.20.0.35
  selector:
    app: boss-dev
  ports:
    - name: ssh
      port: 22
      targetPort: 22
