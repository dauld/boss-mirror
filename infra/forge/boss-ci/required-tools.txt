# Every binary the CI gate invokes, one per line. This is the manifest
# the locomotive check reads: a tool listed here and absent from the
# runner image is a red signal in seconds, not a panic 20 minutes into
# the suite (forge train #1 round 1: python3 missing read as "3 tests
# failed" when the story was "the container can't run what the suite
# runs"). The pair (this file ↔ the Dockerfile's apt list) is pinned by
# the stamp check in locomotive.sh — CLAUDE.md 9a's equality test,
# enforced at run time where the drift actually bites.
node
bun
git
cargo
rustc
rustfmt
cargo-clippy
psql
curl
# tmux — the durable session's multiplexer. `kubectl exec` dies with
# the terminal that opened it; tmux is what the session attaches to.
tmux
# bunx — the symlink beside bun. Absent from the image until
# 2026-08-16, which cost a red train; listed so it cannot go missing
# again without ci-tools-declared.sh noticing.
bunx
# THE CHROMIUM BROWSER IS NOT A LINE HERE, AND THAT IS THE CONVENTION
# WORKING. This file is a list of PATH binaries — locomotive.sh runs
# `command -v` over it — and a Playwright browser is not on PATH: it
# sits under PLAYWRIGHT_BROWSERS_PATH (/opt/ms-playwright, baked) in a
# revision-named directory that only the driver resolves. Writing
# `chromium` here would red every run rather than check anything.
#
# It is checked, just not by this mechanism: locomotive.sh's check 3
# finds the binary under the effective browsers path and RUNS it
# (`chrome --version`), which also proves the sixteen shared libraries
# in the Dockerfile resolve — the failure `command -v` could never see
# and the one a pod restart used to cause (2026-08-21 runbook note).
