# disk-floor-sweep.service (forge host)
[[workflow]]
kind = "maintenance-disk-floor-sweep"
label = "Forge disk floor sweep"
category = "platform"
subject_kinds = ["custom"]
owning_team = "platform"
description = "Hourly: below BOSS_DISK_FLOOR_GB free on the forge host's root volume, reclaims regenerable docker caches in a fixed order (build cache, dangling images, registry-verified old tags) and stops at the floor. Never volumes, never non-docker paths. A run that cannot meet the floor fails the unit and leaves this packet open - the alarm. Exists because cluster-deploy-runner's cleanup only runs when forge main moves, which needs CI, which needs disk (2026-09-02: every train blocked)."
metadata = { owner_role = "platform-admin" }

[[workflow.step]]
title = "scheduled"
kind = "trigger"
ready_when = "true"
title_template = "Timer fired"
metadata_defaults = { trigger_kind = "periodic", trigger_name = "systemd-timer" }

[[workflow.step]]
title = "run"
kind = "task"
ready_when = "steps.scheduled.done"
title_template = "Run to completion"
authority_role = "platform-admin"
fields = [{ name = "result", field_type = "string", required = true }]

[[workflow.step]]
title = "completed"
kind = "outcome"
ready_when = "steps.run.done AND steps.run.metadata.result = \"ok\""
title_template = "Maintenance completed"
metadata_defaults = { outcome_kind = "completed" }
terminal = { outcome = "completed" }

# A run that died records how (boss-step.sh from ExecStopPost: the
# service result and exit status), and lands here instead of looking
# like a run still in progress. Before 2026-09-05 this terminal did
# not exist and a failed run's packet stayed open until a later run
# closed it "ok".
[[workflow.step]]
title = "failed"
kind = "outcome"
ready_when = "steps.run.done AND steps.run.metadata.result != \"ok\""
title_template = "Maintenance failed"
metadata_defaults = { outcome_kind = "aborted" }
terminal = { outcome = "failed" }
